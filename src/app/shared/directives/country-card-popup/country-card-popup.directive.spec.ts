import { CountryCardPopupDirective } from './country-card-popup.directive';

describe('CountryCardPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryCardPopupDirective();
    expect(directive).toBeTruthy();
  });
});
