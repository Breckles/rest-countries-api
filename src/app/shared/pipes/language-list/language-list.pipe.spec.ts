import { LanguageListPipe } from './language-list.pipe';

describe('LanguageListPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageListPipe();
    expect(pipe).toBeTruthy();
  });
});
